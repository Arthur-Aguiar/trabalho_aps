// Declaração e preenchimento de uma matriz
let matriz = [
  [1, 2, 3],
  [4, 5, 6],
  [7, 8, 9]
];

console.log(matriz);

//Neste exemplo, declaramos uma matriz chamada matriz que contém três linhas e três colunas.
//Cada linha é representada por um array, e esses arrays são agrupados em um array maior, formando assim a matriz.