// Vetor de nomes
let nomes = ["Jão", "Zezão", "Chico", "Ana"];

// Matriz de notas dos alunos
let notas = [
  [7, 8, 9],
  [6, 7, 8],
  [9, 9, 10],
  [8, 7, 9]
];


for (let i = 0; i < nomes.length; i++) { //é responsável por percorrer os elementos do vetor 'nomes' utilizando a variável 'i' como contador.
  console.log("Notas:", notas[i]);
  console.log("-------------------");
}

// temos um vetor chamado nomes que armazena os 'nomes' de alguns alunos, e uma matriz chamada 'notas' que armazena as notas desses alunos.